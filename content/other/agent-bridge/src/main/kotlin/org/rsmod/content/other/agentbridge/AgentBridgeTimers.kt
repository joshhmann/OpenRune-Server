package org.rsmod.content.other.agentbridge

/**
 * Timer key for the per-player agent bridge tick loop. Registered via onPlayerSoftTimer in
 * AgentBridgeScript.
 */
internal const val AGENT_BRIDGE_TIMER = "timer.agent_bridge"
